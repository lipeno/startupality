<%= form_for(@instructional_video) do |f| %>
  <% if @instructional_video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instructional_video.errors.count, "error") %> prohibited this instructional_video from being saved:</h2>

      <ul>
      <% @instructional_video.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
